apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-consumption-worker
  namespace: eda-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-consumption-worker
  template:
    metadata:
      labels:
        app: celery-consumption-worker
    spec:
      containers:
      - name: celery
        image: eda-celery-worker:latest
        command: ["celery"]
        args: ["-A", "tasks.consumption_tasks.celery", "worker", "-l", "info", "-Q", "consumption_queue"]
        envFrom:
        - configMapRef:
            name: eda-config
        - secretRef:
            name: eda-secret
