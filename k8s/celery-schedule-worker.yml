apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-schedule-worker
  namespace: eda-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-schedule-worker
  template:
    metadata:
      labels:
        app: celery-schedule-worker
    spec:
      containers:
      - name: celery
        image: eda-celery-worker:latest
        command: ["celery"]
        args: ["-A", "tasks.schedule_tasks.celery", "worker", "-l", "info", "-Q", "schedule_queue"]
        envFrom:
        - configMapRef:
            name: eda-config
        - secretRef:
            name: eda-secret
