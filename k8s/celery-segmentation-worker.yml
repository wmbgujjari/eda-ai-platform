apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-segmentation-worker
  namespace: eda-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-segmentation-worker
  template:
    metadata:
      labels:
        app: celery-segmentation-worker
    spec:
      containers:
      - name: celery
        image: eda-celery-worker:latest
        command: ["celery"]
        args: ["-A", "tasks.segmentation_tasks.celery", "worker", "-l", "info", "-Q", "segmentation_queue"]
        envFrom:
        - configMapRef:
            name: eda-config
        - secretRef:
            name: eda-secret
