apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-revenue-worker
  namespace: eda-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-revenue-worker
  template:
    metadata:
      labels:
        app: celery-revenue-worker
    spec:
      containers:
      - name: celery
        image: eda-celery-worker:latest
        command: ["celery"]
        args: ["-A", "tasks.revenue_tasks.celery", "worker", "-l", "info", "-Q", "revenue_queue"]
        envFrom:
        - configMapRef:
            name: eda-config
        - secretRef:
            name: eda-secret
